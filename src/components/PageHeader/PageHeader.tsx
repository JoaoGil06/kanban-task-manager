import { Header } from './styles/PageHeader.styledcomponent';
import SideBar from '../SideBar';

export const PageHeader = () => {
	return (
		<>
			<Header>Header</Header>
			<SideBar />
		</>
	);
};
